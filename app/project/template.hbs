<h1>{{model.name}}</h1>

{{file-input on-add="addFile"}}

<ul>
  {{#each model.artefacts as |artefact|}}
    <li>
      {{artefact.file.name}}
      {{file-preview file=artefact.file}}
      ({{moment-format artefact.createdAt}})
    </li>
  {{/each}}
</ul>
